spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: user-management-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: user
      username: user
