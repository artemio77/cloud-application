spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: user
      username: user
  main:
    allow-bean-definition-overriding: true
